spring:
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    open-in-view: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL92Dialect
  thymeleaf:
    cache: false
version: 1.2.3-test
